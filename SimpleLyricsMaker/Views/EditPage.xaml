<Page
    x:Class="SimpleLyricsMaker.Views.EditPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:SimpleLyricsMaker.Views"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:interactivity="using:Microsoft.Xaml.Interactivity"
    xmlns:interactions="using:Microsoft.Xaml.Interactions.Core"
    mc:Ignorable="d"
    DataContext="{Binding Source={StaticResource Locator}, Path=Edit}">

    <SplitView x:Name="Root_SplitView" DisplayMode="Inline">
        <SplitView.Pane>
            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition/>
                </Grid.RowDefinitions>

                <Grid>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition/>
                        <ColumnDefinition Width="Auto"/>
                    </Grid.ColumnDefinitions>

                    <TextBox x:Name="Search_TextBox" KeyUp="Search_TextBox_OnKeyUp">
                        <interactivity:Interaction.Behaviors>
                            <interactions:EventTriggerBehavior EventName="LostFocus">
                                <interactions:InvokeCommandAction Command="{Binding SearchFilesCommand}" CommandParameter="{Binding ElementName=Search_TextBox, Path=Text}"/>
                            </interactions:EventTriggerBehavior>
                        </interactivity:Interaction.Behaviors>
                    </TextBox>
                    <Button x:Name="Refresh_Button" Grid.Column="1" Command="{Binding RefreshCommand}" Style="{StaticResource ControlButtonStyle}">
                        <FontIcon Glyph="&#xE117;"/>
                    </Button>
                </Grid>

                <Grid Grid.Row="1">
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition/>
                    </Grid.RowDefinitions>

                    <CheckBox x:Name="ShowAllFile_CheckBox" Content="Show all music file" Command="{Binding SwitchDisplayCommand}" CommandParameter="{Binding ElementName=ShowAllFile_CheckBox, Path=IsChecked}" HorizontalAlignment="Right"/>

                    <ListView x:Name="MusicFiles_ListView" Grid.Row="1" SelectedItem="{Binding CurrentFile, Mode=TwoWay}" ItemsSource="{Binding DisplayFilesList, Mode=OneWay}">
                        <ListView.ItemTemplate>
                            <DataTemplate>
                                <TextBlock Text="{Binding FileName}"/>
                            </DataTemplate>
                        </ListView.ItemTemplate>
                    </ListView>

                    <StackPanel x:Name="Loading_StackPanel" Grid.Row="1" HorizontalAlignment="Center" VerticalAlignment="Center">
                        <ProgressRing x:Name="Loading_ProgressRing" Width="100" Height="100"/>

                        <TextBlock x:Name="LoadingInfo_TextBlock" Style="{ThemeResource SubtitleTextBlockStyle}"/>
                    </StackPanel>
                </Grid>
            </Grid>
        </SplitView.Pane>

        <Grid>
            <StackPanel x:Name="QuickMenu_StackPanel" HorizontalAlignment="Center" VerticalAlignment="Center">
                <StackPanel.Background>
                    <SolidColorBrush Color="{ThemeResource SystemBaseHighColor}" Opacity="0.05"/>
                </StackPanel.Background>
                <StackPanel.Resources>
                    <Style TargetType="Button">
                        <Setter Property="Width" Value="200"/>
                        <Setter Property="Margin" Value="12"/>
                    </Style>
                </StackPanel.Resources>

                <Button Content="Open file" Command="{Binding OpenFileCommand}"/>
                <Button Content="Open folder" Command="{Binding OpenFolderCommand}"/>

            </StackPanel>
        </Grid>
    </SplitView>
</Page>
